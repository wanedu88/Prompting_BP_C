# Cursor AI 프롬프팅 엔지니어링 KPT

> Cursor AI를 활용한 프롬프팅 엔지니어링 경험을 Keep / Problem / Try 관점으로 정리한 문서입니다.

---

## Keep — 계속 이용했으면 하는 좋은 프롬프트 방법

1. **역할·목표·제약을 한 블록으로 명시하기**  
   `[P] 역할`, `[T] 할 일`, `[F] 출력 형식`처럼 역할(Role), Task, Format을 분리해 적으면 모델이 범위를 벗어나는 응답이 줄어듭니다. 특히 “하지 말 것”(범위 밖 수정 금지, 테스트 추가 금지 등)을 Task와 함께 적는 방식이 효과적입니다.

2. **@ 참조로 컨텍스트를 좁히기**  
   파일·폴더·심볼·문서를 `@`로 지정하면 전체 레포 스캔보다 정확하고 빠른 수정이 나옵니다. “이 함수만”, “이 디렉터리만”처럼 작업 단위를 프롬프트와 참조 대상을 일치시키는 습관을 유지합니다.

3. **기대 결과를 검증 가능한 문장으로 쓰기**  
   “개선해줘” 대신 “빌드 통과”, “기존 API 시그니처 유지”, “에러 메시지는 한국어”처럼 통과/실패를 판별할 수 있는 조건을 적습니다. 후속 대화에서도 같은 기준을 재사용하기 쉽습니다.

4. **작은 단위로 쪼개 연속 대화하기**  
   대규모 리팩터링·기능 추가를 한 번에 요청하기보다, 탐색 → 설계 확인 → 구현 → 검증 순으로 턴을 나누면 diff 품질과 설명 품질이 모두 좋아집니다. 각 턴 끝에 “다음에 할 일”을 한 줄로 남기면 맥락 유지에 유리합니다.

5. **프로젝트 규칙(.cursor/rules, AGENTS.md)에 반복 지침 고정하기**  
   코딩 스타일, 커밋 정책, 테스트 원칙 등 매번 프롬프트에 넣던 내용은 규칙 파일로 옮기고, 프롬프트에는 **이번 작업만의 변수**를 적습니다. 규칙과 일회성 지시의 역할 분리가 장기적으로 가장 ROI가 큽니다.

---

## Problem — 개선이 필요한 항목

1. **컨텍스트 과다·과소의 균형**  
   @ 참조를 너무 많이 걸면 노이즈와 토큰 낭비가 생기고, 너무 적으면 잘못된 파일을 수정합니다. “필요한 최소 컨텍스트” 기준을 팀 단위로 공유·문서화할 필요가 있습니다.

2. **대화 히스토리에 의존한 암묵적 전제**  
   이전 턴에서만 합의된 제약(라이브러리 버전, 아키텍처 결정)이 새 세션·새 채팅에 전달되지 않아 같은 실수가 반복됩니다. 세션 경계에서 컨텍스트가 끊기는 것이 구조적 문제입니다.

3. **에이전트 자율 실행과 통제의 트레이드오프**  
   긴 작업을 한 번에 맡기면 범위 밖 파일 수정·불필요한 리팩터링·과한 테스트 추가가 발생할 수 있습니다. “최소 변경”, “질문 후 진행” 같은 제약을 프롬프트만으로는 항상 지키기 어렵습니다.


---

## Try — 새로운 시도를 해볼 수 있는 방법

1. **프롬프트 템플릿 라이브러리화**  
   자주 쓰는 패턴(버그 수정, API 추가, 코드 리뷰, 문서화)을 `prompts/` 또는 이 레포의 템플릿 MD로 두고, `[P][T][F]` 슬롯만 채워 쓰는 방식을 시도합니다.

2. **Plan 모드 → Agent 모드 2단계 워크플로**  
   큰 변경은 먼저 Plan(또는 설계-only) 턴에서 접근·파일 목록·리스크를 확정한 뒤, Agent 턴에서 구현하도록 분리해 범위 이탈을 줄입니다.

3. **Composer / 멀티파일 작업 전 체크리스트 프롬프트**  
   구현 전에 “수정 예상 파일 목록”, “건드리지 않을 경로”, “검증 명령(`npm test`, `pytest` 등)”을 프롬프트 상단에 고정하는 체크리스트를 표준화합니다.

4. **Cursor Rules + Skills 조합**  
   반복 업무(커밋 메시지, PR 본문, 린트 수정)는 Skill로, 프로젝트 고유 규칙은 Rules로 나누어 “프롬프트는 이번 작업 변수만” 전략을 강화합니다.


