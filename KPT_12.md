# Cursor AI 실습 KPT 회고

| 항목 | 내용 |
|---|---|
| 실습 주제 | Cursor AI를 활용한 C++17 리팩토링 및 테스트 자동화 |
| 주요 도구 | Cursor AI, @Codebase, Composer, Chat, Google Test, CMake |
| 회고 방식 | Keep / Problem / Try |
| 작성 목적 | AI를 단순 코드 생성 도구가 아니라 코드 분석, 리팩토링, 테스트 검증 보조 도구로 활용하기 위한 개선점 정리 |

---

## 1. Keep

이번 실습에서 효과가 있었고 앞으로도 계속 유지하고 싶은 방식입니다.

### 1.1 코드 수정 전에 구조부터 파악하기

Cursor AI를 사용할 때 바로 “수정해줘”라고 요청하기보다, 먼저 `@Codebase`나 특정 파일 참조를 통해 전체 구조를 파악하는 방식이 효과적이었습니다.

특히 C++ 프로젝트에서는 하나의 파일만 보고 판단하면 헤더 파일, 구현 파일, CMake 설정, 테스트 코드와의 연결 관계를 놓치기 쉽습니다. 따라서 수정 전에 다음 순서로 접근하는 것이 좋았습니다.

```text
1. 전체 구조 파악
2. 주요 클래스와 함수 역할 확인
3. 변경 범위 결정
4. 실제 코드 수정
5. 빌드 및 테스트 검증
```

이 방식은 AI가 불필요한 파일을 건드리거나, 프로젝트 구조와 맞지 않는 코드를 제안하는 상황을 줄이는 데 도움이 되었습니다.

### 1.2 프롬프트에 환경 정보를 명확히 적기

C++ 실습에서는 언어 표준과 빌드 환경을 명확히 적는 것이 중요했습니다.

예를 들어 단순히 “리팩토링해줘”라고 하는 것보다 아래처럼 작성했을 때 더 실습 환경에 맞는 결과를 얻을 수 있었습니다.

```text
C++17 기준으로 작성해주세요.
현재 프로젝트는 CMake 기반입니다.
테스트는 Google Test를 사용합니다.
기존 public 함수 시그니처는 유지해주세요.
수정 후 ctest --output-on-failure 기준으로 검증 가능한 형태로 작성해주세요.
```

이처럼 언어, 버전, 빌드 도구, 테스트 도구를 명시하면 AI가 현재 프로젝트와 맞지 않는 코드를 생성할 가능성을 줄일 수 있었습니다.

### 1.3 테스트 가능성을 기준으로 리팩토링하기

이번 실습에서 가장 도움이 되었던 기준은 “코드가 깔끔한가?”보다 “테스트하기 쉬운 구조인가?”였습니다.

AI에게 리팩토링을 요청할 때도 단순히 코드 줄 수를 줄이는 것보다 다음 기준을 함께 요청하는 것이 좋았습니다.

- 함수가 너무 많은 책임을 갖고 있지 않은가?
- 입력과 출력이 명확한가?
- 외부 의존성을 분리할 수 있는가?
- Google Test로 검증 가능한 단위인가?
- 기존 동작을 유지하면서 구조만 개선할 수 있는가?

이 기준을 넣으면 AI가 단순 정리 수준이 아니라 테스트 가능한 구조로 개선 방향을 제안해 주었습니다.

### 1.4 변경 후 빌드와 테스트를 반드시 확인하기

AI가 생성한 코드는 바로 정답으로 보기 어렵습니다. 특히 C++에서는 컴파일 오류, 링크 오류, include 누락, CMake 설정 문제 등이 쉽게 발생할 수 있습니다.

따라서 코드 생성 이후에는 반드시 다음 명령으로 검증하는 흐름을 유지해야 합니다.

```bash
cmake --build build
ctest --output-on-failure
```

프롬프트에도 “수정 후 빌드와 테스트가 통과하는지 확인할 수 있도록 작성해줘”라고 명시하는 방식이 효과적이었습니다.

---

## 2. Problem

실습 중 불편했거나 개선이 필요하다고 느낀 점입니다.

### 2.1 AI가 프로젝트에 없는 구조를 가정하는 경우

AI가 실제 코드베이스에 없는 함수, 클래스, 파일명을 임의로 가정하는 경우가 있었습니다.

예를 들어 다음과 같은 문제가 발생할 수 있습니다.

- 존재하지 않는 helper 함수 호출
- 실제 프로젝트에 없는 테스트 fixture 사용
- CMake에 등록되지 않은 파일을 기준으로 설명
- 현재 C++17 환경과 맞지 않는 문법 제안

이 문제는 AI가 전체 프로젝트 구조를 정확히 알고 있다고 착각할 때 더 자주 발생했습니다.

### 2.2 큰 범위의 요청은 결과 품질이 떨어짐

“전체 코드 리팩토링해줘”처럼 범위가 큰 요청은 결과가 불안정했습니다.

한 번에 너무 많은 것을 요청하면 다음 문제가 생겼습니다.

- 수정 범위가 예상보다 커짐
- 원래 의도와 다른 구조 변경 발생
- 테스트 코드와 실제 코드가 어긋남
- 변경 이유를 추적하기 어려움
- Diff 검토가 어려워짐

특히 Composer나 Agent 방식으로 여러 파일을 동시에 수정할 때는 작업 범위를 더 작게 나누는 것이 필요했습니다.

### 2.3 모호한 표현은 모호한 결과를 만듦

“좋게 바꿔줘”, “깔끔하게 해줘”, “문제 찾아줘” 같은 표현은 기대한 수준의 결과를 만들기 어려웠습니다.

AI는 기준이 없으면 일반적인 코드 스타일 이야기만 하거나, 실제 프로젝트에서 중요하지 않은 부분을 지적하는 경우가 있었습니다.

따라서 다음과 같이 기준을 구체화해야 했습니다.

```text
SOLID 관점에서 문제를 찾아줘.
테스트 가능성을 기준으로 개선점을 제안해줘.
기존 API 시그니처는 유지해줘.
Critical / Major / Minor로 나눠서 설명해줘.
```

### 2.4 Accept All은 위험할 수 있음

AI가 제안한 변경을 한 번에 모두 수락하면 의도하지 않은 파일까지 변경될 수 있습니다.

특히 아래 파일은 작은 변경도 프로젝트 전체에 영향을 줄 수 있으므로 주의가 필요했습니다.

- `CMakeLists.txt`
- 테스트 설정 파일
- 공통 헤더 파일
- main 진입점 파일
- 외부 라이브러리 설정 파일

따라서 AI가 만든 변경사항은 반드시 파일별로 Diff를 확인한 뒤 반영해야 합니다.

---

## 3. Try

앞으로 실습이나 현업에서 새롭게 시도해볼 개선 방법입니다.

### 3.1 C++ 리팩토링 전용 프롬프트 템플릿 사용

앞으로 C++ 코드 리팩토링을 요청할 때는 아래 템플릿을 기본으로 사용합니다.

```text
당신은 C++17 기반 프로젝트를 다루는 시니어 소프트웨어 엔지니어입니다.

[대상]
@수정할파일명

[환경]
- C++17
- CMake 기반 빌드
- Google Test 사용
- 기존 public API 시그니처 유지

[작업]
- 코드 스멜을 찾아주세요.
- 책임이 많은 함수나 클래스를 분리해주세요.
- 테스트 가능한 구조로 개선해주세요.
- 불필요하게 큰 변경은 하지 말아주세요.

[제약]
- 프로젝트에 없는 함수나 클래스를 임의로 만들지 마세요.
- 수정이 필요한 파일 목록을 먼저 제시해주세요.
- CMake 설정 변경이 필요하면 이유를 설명해주세요.

[출력]
- 문제점 요약
- 수정 방향
- 변경 예상 파일
- Before / After 예시
- 빌드 및 테스트 명령어
```

### 3.2 작업을 4단계로 나누기

큰 작업을 한 번에 AI에게 맡기지 않고 아래 4단계로 나누어 진행합니다.

```text
1단계: 분석만 요청
2단계: 수정 계획 확인
3단계: 실제 코드 수정
4단계: 빌드 및 테스트 검증
```

이렇게 나누면 각 단계에서 개발자가 방향을 확인할 수 있고, AI가 잘못된 방향으로 많은 파일을 수정하는 문제를 줄일 수 있습니다.

### 3.3 팀 공용 Cursor 규칙 정리

반복적으로 사용하는 지침은 매번 프롬프트에 적기보다 팀 공용 규칙 파일로 관리합니다.

예를 들어 다음 내용을 규칙화할 수 있습니다.

```text
- C++ 코드는 C++17 기준으로 작성한다.
- 기존 public API 시그니처는 유지한다.
- 리팩토링 시 기능 변경과 구조 변경을 분리한다.
- 테스트는 Google Test 기준으로 작성한다.
- 수정 후 cmake --build build, ctest --output-on-failure 기준으로 검증한다.
- Accept All을 사용하지 않고 파일별 Diff를 확인한다.
```

이를 통해 팀원마다 다른 방식으로 AI를 사용하는 문제를 줄이고, 일관된 결과를 얻을 수 있습니다.

### 3.4 테스트 요청을 더 구체화하기

테스트 코드를 요청할 때도 단순히 “테스트 만들어줘”가 아니라 테스트 기준을 명확히 작성합니다.

```text
@대상파일 /test

Google Test 기반 테스트를 작성해주세요.

포함할 테스트:
- 정상 입력
- 경계값 입력
- 잘못된 입력
- 예외 상황
- 기존 동작 유지 여부

테스트 구조:
- Given / When / Then 주석 포함
- 테스트 이름은 동작이 드러나게 작성
- ctest --output-on-failure로 실행 가능하게 작성
```

이 방식은 테스트 코드의 품질을 높이고, 리팩토링 이후 기능이 유지되는지 확인하는 데 도움이 됩니다.

---

## 4. 정리

이번 실습을 통해 Cursor AI는 코드를 대신 작성해주는 도구라기보다, 개발자의 분석과 검증을 빠르게 도와주는 보조 도구에 가깝다는 것을 느꼈습니다.

좋은 결과를 얻기 위해서는 AI에게 많은 것을 한 번에 맡기는 것보다, 개발자가 작업 범위와 기준을 명확히 정해주는 것이 중요했습니다.

앞으로는 다음 원칙을 유지하며 AI를 활용하고자 합니다.

```text
1. 수정 전에 구조를 먼저 파악한다.
2. 언어, 버전, 빌드 환경을 명확히 적는다.
3. 작업 범위를 작게 나눈다.
4. AI 결과는 반드시 빌드와 테스트로 검증한다.
5. 반복 지침은 팀 공용 규칙으로 관리한다.
```

이 원칙을 기반으로 Cursor AI를 코드 리뷰, 리팩토링, 테스트 자동화, 문서화 과정에 점진적으로 적용할 계획입니다.
